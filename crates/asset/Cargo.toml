[package]
name = "asset"
version = "0.1.0"
authors = ["Colby <me@cobeh.com>"]
edition = "2018"

[dependencies]
core = { path = "../core" }
log  = { path = "../log" }

# For auto generated serialization
serde = { version = "1.0", features = ["derive"] }
ron = "*" # RON format for assets