[package]
name = "egui"
version = "0.1.0"
edition = "2021"

[lib]
path = "egui.rs"

[dependencies]
gpu = { path = "../../runtime/gpu" }
resources = { path = "../../runtime/resources" }
engine = { path = "../../runtime/engine" }
newport_math = { path = "../../runtime/math", package = "math" }
os = { path = "../../runtime/os" }

base = { version = "0.15", package = "egui" }