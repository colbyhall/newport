[package]
name = "serde"
version = "0.2.0"
authors = ["Colby <me@cobeh.com>"]
edition = "2018"
license = "Apache-2.0"
repository = "https://github.com/colbyhall/newport"
description = "Newport wrapper around serde"
categories = ["game-engines", "game-development"]
keywords = ["game", "engine", "gamedev", "newport"]

[lib]
path = "serde.rs"

[dependencies]
ron = "0.6.4"
bincode = "1.3.3"
base = { version = "1.0.125", features = ["derive"], package = "serde" }